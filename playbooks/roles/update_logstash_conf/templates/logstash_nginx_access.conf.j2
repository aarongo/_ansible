input {
  file{
    type=>"nginx-access"
    path=>"{{nginx_logs_path}}{{ansible_hostname}}f.cdc.carrefour.com_access.log"
    codec => json {
      charset => "UTF-8"
    }
  }
}
filter {
  if [type] == 'nginx-access' {
    json {
      source => "@fields"
    }
  }
}
output {
    redis {
      host => "10.171.35.25"
      port => "6379"
      db => "0"
      data_type => "list"
      key => "nginx:redis"
    }
}